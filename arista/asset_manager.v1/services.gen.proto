// Copyright (c) 2024 Arista Networks, Inc.  All rights reserved.
// Use of this source code is governed by the Apache License 2.0
// that can be found in the COPYING file.

//
// Code generated by boomtown. DO NOT EDIT.
//

syntax = "proto3";

package arista.asset_manager.v1;
option go_package = "github.com/aristanetworks/cloudvision-go/api/arista/asset_manager.v1;asset_manager";

option java_package = "com.arista.asset_manager.v1";
option java_outer_classname = "AssetManagerServices";
option java_multiple_files = true;

import "arista/asset_manager.v1/asset_manager.proto";
import "arista/time/time.proto";
import "arista/subscriptions/subscriptions.proto";
import "fmp/deletes.proto";
import "fmp/pages.proto";
import "arista/license.v1/license.proto";
import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

message MetaResponse {
	// Time holds the timestamp of the last item included in the metadata calculation.
	google.protobuf.Timestamp time = 1;

	// Operation indicates how the value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 2;

	// Count is the number of items present under the conditions of the request.
	google.protobuf.UInt32Value count = 3;
};

message AllowedTenantsRequest {
	// Key uniquely identifies a AllowedTenants instance to retrieve.
	// This value must be populated.
	TenantKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message AllowedTenantsResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	AllowedTenants value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// AllowedTenants instance in this response.
	google.protobuf.Timestamp time = 2;
};

message AllowedTenantsSomeRequest {
	repeated TenantKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message AllowedTenantsSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	AllowedTenants value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	// Time carries the (UTC) timestamp of the last-modification of the
	// AllowedTenants instance in this response.
	google.protobuf.Timestamp time = 3;
};

message AllowedTenantsStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated AllowedTenants partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each AllowedTenants at end.
	//     * Each AllowedTenants response is fully-specified (all fields set).
	//   * start: Returns the state of each AllowedTenants at start, followed by updates until now.
	//     * Each AllowedTenants response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each AllowedTenants at start, followed by updates
	//     until end.
	//     * Each AllowedTenants response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message AllowedTenantsStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	AllowedTenants value = 1;

	// Time holds the timestamp of this AllowedTenants's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the AllowedTenants value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message AllowedTenantsBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated AllowedTenants partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each AllowedTenants at end.
	//     * Each AllowedTenants response is fully-specified (all fields set).
	//   * start: Returns the state of each AllowedTenants at start, followed by updates until now.
	//     * Each AllowedTenants response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each AllowedTenants at start, followed by updates
	//     until end.
	//     * Each AllowedTenants response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message AllowedTenantsBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated AllowedTenantsStreamResponse responses = 1;
};

service AllowedTenantsService {
	rpc GetOne (AllowedTenantsRequest) returns (AllowedTenantsResponse);
	rpc GetSome (AllowedTenantsSomeRequest) returns (stream AllowedTenantsSomeResponse);
	rpc GetAll (AllowedTenantsStreamRequest) returns (stream AllowedTenantsStreamResponse);
	rpc Subscribe (AllowedTenantsStreamRequest) returns (stream AllowedTenantsStreamResponse);
	rpc GetMeta (AllowedTenantsStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (AllowedTenantsStreamRequest) returns (stream MetaResponse);
	rpc GetAllBatched (AllowedTenantsBatchedStreamRequest) returns (stream AllowedTenantsBatchedStreamResponse);
	rpc SubscribeBatched (AllowedTenantsBatchedStreamRequest) returns (stream AllowedTenantsBatchedStreamResponse);
}

message DeviceTenantAssignConfigRequest {
	// Key uniquely identifies a DeviceTenantAssignConfig instance to retrieve.
	// This value must be populated.
	DeviceKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message DeviceTenantAssignConfigResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	DeviceTenantAssignConfig value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// DeviceTenantAssignConfig instance in this response.
	google.protobuf.Timestamp time = 2;
};

message DeviceTenantAssignConfigSomeRequest {
	repeated DeviceKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message DeviceTenantAssignConfigSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	DeviceTenantAssignConfig value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	// Time carries the (UTC) timestamp of the last-modification of the
	// DeviceTenantAssignConfig instance in this response.
	google.protobuf.Timestamp time = 3;
};

message DeviceTenantAssignConfigStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated DeviceTenantAssignConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each DeviceTenantAssignConfig at end.
	//     * Each DeviceTenantAssignConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each DeviceTenantAssignConfig at start, followed by updates until now.
	//     * Each DeviceTenantAssignConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each DeviceTenantAssignConfig at start, followed by updates
	//     until end.
	//     * Each DeviceTenantAssignConfig response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message DeviceTenantAssignConfigStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	DeviceTenantAssignConfig value = 1;

	// Time holds the timestamp of this DeviceTenantAssignConfig's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the DeviceTenantAssignConfig value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message DeviceTenantAssignConfigBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated DeviceTenantAssignConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each DeviceTenantAssignConfig at end.
	//     * Each DeviceTenantAssignConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each DeviceTenantAssignConfig at start, followed by updates until now.
	//     * Each DeviceTenantAssignConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each DeviceTenantAssignConfig at start, followed by updates
	//     until end.
	//     * Each DeviceTenantAssignConfig response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message DeviceTenantAssignConfigBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated DeviceTenantAssignConfigStreamResponse responses = 1;
};

message DeviceTenantAssignConfigSetRequest {
	// DeviceTenantAssignConfig carries the value to set into the datastore.
	// See the documentation on the DeviceTenantAssignConfig struct for which fields are required.
	DeviceTenantAssignConfig value = 1;
};

message DeviceTenantAssignConfigSetResponse {
	// Value carries all the values given in the DeviceTenantAssignConfigSetRequest as well
	// as any server-generated values.
	DeviceTenantAssignConfig value = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// creation. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==CreatedAt will include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message DeviceTenantAssignConfigSetSomeRequest {
	// value contains a list of DeviceTenantAssignConfig values to write.
	// It is possible to provide more values than can fit within either:
	//     - the maxiumum send size of the client
	//     - the maximum receive size of the server
	// If this error occurs you must reduce the number of values sent.
	// See gRPC "maximum message size" documentation for more information.
	repeated DeviceTenantAssignConfig values = 1;
};

message DeviceTenantAssignConfigSetSomeResponse {
	DeviceKey key = 1;
	string error = 2;
};

message DeviceTenantAssignConfigDeleteRequest {
	// Key indicates which DeviceTenantAssignConfig instance to remove.
	// This field must always be set.
	DeviceKey key = 1;
};

message DeviceTenantAssignConfigDeleteResponse {
	// Key echoes back the key of the deleted DeviceTenantAssignConfig instance.
	DeviceKey key = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// deletion. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==DeletedAt will not include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message DeviceTenantAssignConfigDeleteSomeRequest {
	// key contains a list of DeviceTenantAssignConfig keys to delete
	repeated DeviceKey keys = 1;
};

// DeviceTenantAssignConfigDeleteSomeResponse is only sent when there is an error.
message DeviceTenantAssignConfigDeleteSomeResponse {
	DeviceKey key = 1;
	string error = 2;
};

message DeviceTenantAssignConfigDeleteAllRequest {
	// PartialEqFilter provides a way to server-side filter a DeleteAll.
	// This requires all provided fields to be equal to the response.
	// A filtered DeleteAll will use GetAll with filter to find things to delete.
	repeated DeviceTenantAssignConfig partial_eq_filter = 1;
};

message DeviceTenantAssignConfigDeleteAllResponse {
	// This describes the class of delete error.
	// A DeleteAllResponse is only sent when there is an error.
	fmp.DeleteError type = 1;
	// This indicates the error message from the delete failure.
	google.protobuf.StringValue error = 2;
	// This is the key of the DeviceTenantAssignConfig instance that failed to be deleted.
	DeviceKey key = 3;
	// Time indicates the (UTC) timestamp when the key was being deleted.
	google.protobuf.Timestamp time = 4;
};

service DeviceTenantAssignConfigService {
	rpc GetOne (DeviceTenantAssignConfigRequest) returns (DeviceTenantAssignConfigResponse);
	rpc GetSome (DeviceTenantAssignConfigSomeRequest) returns (stream DeviceTenantAssignConfigSomeResponse);
	rpc GetAll (DeviceTenantAssignConfigStreamRequest) returns (stream DeviceTenantAssignConfigStreamResponse);
	rpc Subscribe (DeviceTenantAssignConfigStreamRequest) returns (stream DeviceTenantAssignConfigStreamResponse);
	rpc GetMeta (DeviceTenantAssignConfigStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (DeviceTenantAssignConfigStreamRequest) returns (stream MetaResponse);
	rpc Set (DeviceTenantAssignConfigSetRequest) returns (DeviceTenantAssignConfigSetResponse);
	rpc SetSome (DeviceTenantAssignConfigSetSomeRequest) returns (stream DeviceTenantAssignConfigSetSomeResponse);
	rpc Delete (DeviceTenantAssignConfigDeleteRequest) returns (DeviceTenantAssignConfigDeleteResponse);
	rpc DeleteSome (DeviceTenantAssignConfigDeleteSomeRequest) returns (stream DeviceTenantAssignConfigDeleteSomeResponse);
	rpc DeleteAll (DeviceTenantAssignConfigDeleteAllRequest) returns (stream DeviceTenantAssignConfigDeleteAllResponse);
	rpc GetAllBatched (DeviceTenantAssignConfigBatchedStreamRequest) returns (stream DeviceTenantAssignConfigBatchedStreamResponse);
	rpc SubscribeBatched (DeviceTenantAssignConfigBatchedStreamRequest) returns (stream DeviceTenantAssignConfigBatchedStreamResponse);
}

message DeviceTenantAssignmentRequest {
	// Key uniquely identifies a DeviceTenantAssignment instance to retrieve.
	// This value must be populated.
	DeviceKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message DeviceTenantAssignmentResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	DeviceTenantAssignment value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// DeviceTenantAssignment instance in this response.
	google.protobuf.Timestamp time = 2;
};

message DeviceTenantAssignmentSomeRequest {
	repeated DeviceKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message DeviceTenantAssignmentSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	DeviceTenantAssignment value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	// Time carries the (UTC) timestamp of the last-modification of the
	// DeviceTenantAssignment instance in this response.
	google.protobuf.Timestamp time = 3;
};

message DeviceTenantAssignmentStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated DeviceTenantAssignment partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each DeviceTenantAssignment at end.
	//     * Each DeviceTenantAssignment response is fully-specified (all fields set).
	//   * start: Returns the state of each DeviceTenantAssignment at start, followed by updates until now.
	//     * Each DeviceTenantAssignment response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each DeviceTenantAssignment at start, followed by updates
	//     until end.
	//     * Each DeviceTenantAssignment response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message DeviceTenantAssignmentStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	DeviceTenantAssignment value = 1;

	// Time holds the timestamp of this DeviceTenantAssignment's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the DeviceTenantAssignment value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message DeviceTenantAssignmentBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated DeviceTenantAssignment partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each DeviceTenantAssignment at end.
	//     * Each DeviceTenantAssignment response is fully-specified (all fields set).
	//   * start: Returns the state of each DeviceTenantAssignment at start, followed by updates until now.
	//     * Each DeviceTenantAssignment response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each DeviceTenantAssignment at start, followed by updates
	//     until end.
	//     * Each DeviceTenantAssignment response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message DeviceTenantAssignmentBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated DeviceTenantAssignmentStreamResponse responses = 1;
};

enum DeviceTenantAssignmentSortableField {
	DEVICE_TENANT_ASSIGNMENT_SORTABLE_FIELD_UNSPECIFIED = 0;
	DEVICE_TENANT_ASSIGNMENT_SORTABLE_FIELD_KEY_DEVICE_ID = -1475536042;
	DEVICE_TENANT_ASSIGNMENT_SORTABLE_FIELD_MODEL_NAME = -1529362212;
	DEVICE_TENANT_ASSIGNMENT_SORTABLE_FIELD_TENANT_ID = 590923987;
	DEVICE_TENANT_ASSIGNMENT_SORTABLE_FIELD_SHIPPING_DATE = 190779440;
	DEVICE_TENANT_ASSIGNMENT_SORTABLE_FIELD_SALES_ORDER_NUMBER = -988583176;
}

message DeviceTenantAssignmentPageRequest {
	// Page is the window-index to fetch.
	uint64 page = 1;
	// Page_length is the size of the page. Combined with page, this will return:
	//    start = (page * page_length)
	//    end = start + page_length
	//    return sorted_table[start..end]
	// Page length should be consistent across requests for the same client-context
	uint64 page_length = 2;

	// Sort_field is the field or subfield of the resource to sort by
	DeviceTenantAssignmentSortableField sort_field = 3;

	// Sort_dir is the direction of the sort, ascending or descending
	fmp.SortDirection sort_dir = 4;

	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated DeviceTenantAssignment partial_eq_filter = 5;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetPage, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each DeviceTenantAssignment at end.
	//     * Each DeviceTenantAssignment response is fully-specified (all fields set).
	//   * start: Returns the state of each DeviceTenantAssignment at start, followed by updates until now.
	//     * Each DeviceTenantAssignment response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each DeviceTenantAssignment at start, followed by updates
	//     until end.
	//     * Each DeviceTenantAssignment response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 7;
};

service DeviceTenantAssignmentService {
	rpc GetOne (DeviceTenantAssignmentRequest) returns (DeviceTenantAssignmentResponse);
	rpc GetSome (DeviceTenantAssignmentSomeRequest) returns (stream DeviceTenantAssignmentSomeResponse);
	rpc GetAll (DeviceTenantAssignmentStreamRequest) returns (stream DeviceTenantAssignmentStreamResponse);
	rpc Subscribe (DeviceTenantAssignmentStreamRequest) returns (stream DeviceTenantAssignmentStreamResponse);
	rpc GetMeta (DeviceTenantAssignmentStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (DeviceTenantAssignmentStreamRequest) returns (stream MetaResponse);
	rpc GetAllBatched (DeviceTenantAssignmentBatchedStreamRequest) returns (stream DeviceTenantAssignmentBatchedStreamResponse);
	rpc SubscribeBatched (DeviceTenantAssignmentBatchedStreamRequest) returns (stream DeviceTenantAssignmentBatchedStreamResponse);
	rpc GetPage (DeviceTenantAssignmentPageRequest) returns (stream DeviceTenantAssignmentResponse);
}

message LicenseTenantAssignConfigRequest {
	// Key uniquely identifies a LicenseTenantAssignConfig instance to retrieve.
	// This value must be populated.
	arista.license.v1.PurchasedLicenseKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message LicenseTenantAssignConfigResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	LicenseTenantAssignConfig value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// LicenseTenantAssignConfig instance in this response.
	google.protobuf.Timestamp time = 2;
};

message LicenseTenantAssignConfigSomeRequest {
	repeated arista.license.v1.PurchasedLicenseKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message LicenseTenantAssignConfigSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	LicenseTenantAssignConfig value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	// Time carries the (UTC) timestamp of the last-modification of the
	// LicenseTenantAssignConfig instance in this response.
	google.protobuf.Timestamp time = 3;
};

message LicenseTenantAssignConfigStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated LicenseTenantAssignConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each LicenseTenantAssignConfig at end.
	//     * Each LicenseTenantAssignConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each LicenseTenantAssignConfig at start, followed by updates until now.
	//     * Each LicenseTenantAssignConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each LicenseTenantAssignConfig at start, followed by updates
	//     until end.
	//     * Each LicenseTenantAssignConfig response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message LicenseTenantAssignConfigStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	LicenseTenantAssignConfig value = 1;

	// Time holds the timestamp of this LicenseTenantAssignConfig's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the LicenseTenantAssignConfig value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message LicenseTenantAssignConfigBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated LicenseTenantAssignConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each LicenseTenantAssignConfig at end.
	//     * Each LicenseTenantAssignConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each LicenseTenantAssignConfig at start, followed by updates until now.
	//     * Each LicenseTenantAssignConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each LicenseTenantAssignConfig at start, followed by updates
	//     until end.
	//     * Each LicenseTenantAssignConfig response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message LicenseTenantAssignConfigBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated LicenseTenantAssignConfigStreamResponse responses = 1;
};

message LicenseTenantAssignConfigSetRequest {
	// LicenseTenantAssignConfig carries the value to set into the datastore.
	// See the documentation on the LicenseTenantAssignConfig struct for which fields are required.
	LicenseTenantAssignConfig value = 1;
};

message LicenseTenantAssignConfigSetResponse {
	// Value carries all the values given in the LicenseTenantAssignConfigSetRequest as well
	// as any server-generated values.
	LicenseTenantAssignConfig value = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// creation. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==CreatedAt will include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message LicenseTenantAssignConfigSetSomeRequest {
	// value contains a list of LicenseTenantAssignConfig values to write.
	// It is possible to provide more values than can fit within either:
	//     - the maxiumum send size of the client
	//     - the maximum receive size of the server
	// If this error occurs you must reduce the number of values sent.
	// See gRPC "maximum message size" documentation for more information.
	repeated LicenseTenantAssignConfig values = 1;
};

message LicenseTenantAssignConfigSetSomeResponse {
	arista.license.v1.PurchasedLicenseKey key = 1;
	string error = 2;
};

message LicenseTenantAssignConfigDeleteRequest {
	// Key indicates which LicenseTenantAssignConfig instance to remove.
	// This field must always be set.
	arista.license.v1.PurchasedLicenseKey key = 1;
};

message LicenseTenantAssignConfigDeleteResponse {
	// Key echoes back the key of the deleted LicenseTenantAssignConfig instance.
	arista.license.v1.PurchasedLicenseKey key = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// deletion. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==DeletedAt will not include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message LicenseTenantAssignConfigDeleteSomeRequest {
	// key contains a list of LicenseTenantAssignConfig keys to delete
	repeated arista.license.v1.PurchasedLicenseKey keys = 1;
};

// LicenseTenantAssignConfigDeleteSomeResponse is only sent when there is an error.
message LicenseTenantAssignConfigDeleteSomeResponse {
	arista.license.v1.PurchasedLicenseKey key = 1;
	string error = 2;
};

message LicenseTenantAssignConfigDeleteAllRequest {
	// PartialEqFilter provides a way to server-side filter a DeleteAll.
	// This requires all provided fields to be equal to the response.
	// A filtered DeleteAll will use GetAll with filter to find things to delete.
	repeated LicenseTenantAssignConfig partial_eq_filter = 1;
};

message LicenseTenantAssignConfigDeleteAllResponse {
	// This describes the class of delete error.
	// A DeleteAllResponse is only sent when there is an error.
	fmp.DeleteError type = 1;
	// This indicates the error message from the delete failure.
	google.protobuf.StringValue error = 2;
	// This is the key of the LicenseTenantAssignConfig instance that failed to be deleted.
	arista.license.v1.PurchasedLicenseKey key = 3;
	// Time indicates the (UTC) timestamp when the key was being deleted.
	google.protobuf.Timestamp time = 4;
};

service LicenseTenantAssignConfigService {
	rpc GetOne (LicenseTenantAssignConfigRequest) returns (LicenseTenantAssignConfigResponse);
	rpc GetSome (LicenseTenantAssignConfigSomeRequest) returns (stream LicenseTenantAssignConfigSomeResponse);
	rpc GetAll (LicenseTenantAssignConfigStreamRequest) returns (stream LicenseTenantAssignConfigStreamResponse);
	rpc Subscribe (LicenseTenantAssignConfigStreamRequest) returns (stream LicenseTenantAssignConfigStreamResponse);
	rpc GetMeta (LicenseTenantAssignConfigStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (LicenseTenantAssignConfigStreamRequest) returns (stream MetaResponse);
	rpc Set (LicenseTenantAssignConfigSetRequest) returns (LicenseTenantAssignConfigSetResponse);
	rpc SetSome (LicenseTenantAssignConfigSetSomeRequest) returns (stream LicenseTenantAssignConfigSetSomeResponse);
	rpc Delete (LicenseTenantAssignConfigDeleteRequest) returns (LicenseTenantAssignConfigDeleteResponse);
	rpc DeleteSome (LicenseTenantAssignConfigDeleteSomeRequest) returns (stream LicenseTenantAssignConfigDeleteSomeResponse);
	rpc DeleteAll (LicenseTenantAssignConfigDeleteAllRequest) returns (stream LicenseTenantAssignConfigDeleteAllResponse);
	rpc GetAllBatched (LicenseTenantAssignConfigBatchedStreamRequest) returns (stream LicenseTenantAssignConfigBatchedStreamResponse);
	rpc SubscribeBatched (LicenseTenantAssignConfigBatchedStreamRequest) returns (stream LicenseTenantAssignConfigBatchedStreamResponse);
}

message LicenseTenantAssignmentRequest {
	// Key uniquely identifies a LicenseTenantAssignment instance to retrieve.
	// This value must be populated.
	arista.license.v1.PurchasedLicenseKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message LicenseTenantAssignmentResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	LicenseTenantAssignment value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// LicenseTenantAssignment instance in this response.
	google.protobuf.Timestamp time = 2;
};

message LicenseTenantAssignmentSomeRequest {
	repeated arista.license.v1.PurchasedLicenseKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message LicenseTenantAssignmentSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	LicenseTenantAssignment value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	// Time carries the (UTC) timestamp of the last-modification of the
	// LicenseTenantAssignment instance in this response.
	google.protobuf.Timestamp time = 3;
};

message LicenseTenantAssignmentStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated LicenseTenantAssignment partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each LicenseTenantAssignment at end.
	//     * Each LicenseTenantAssignment response is fully-specified (all fields set).
	//   * start: Returns the state of each LicenseTenantAssignment at start, followed by updates until now.
	//     * Each LicenseTenantAssignment response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each LicenseTenantAssignment at start, followed by updates
	//     until end.
	//     * Each LicenseTenantAssignment response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message LicenseTenantAssignmentStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	LicenseTenantAssignment value = 1;

	// Time holds the timestamp of this LicenseTenantAssignment's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the LicenseTenantAssignment value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message LicenseTenantAssignmentBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated LicenseTenantAssignment partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each LicenseTenantAssignment at end.
	//     * Each LicenseTenantAssignment response is fully-specified (all fields set).
	//   * start: Returns the state of each LicenseTenantAssignment at start, followed by updates until now.
	//     * Each LicenseTenantAssignment response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each LicenseTenantAssignment at start, followed by updates
	//     until end.
	//     * Each LicenseTenantAssignment response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message LicenseTenantAssignmentBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated LicenseTenantAssignmentStreamResponse responses = 1;
};

enum LicenseTenantAssignmentSortableField {
	LICENSE_TENANT_ASSIGNMENT_SORTABLE_FIELD_UNSPECIFIED = 0;
	LICENSE_TENANT_ASSIGNMENT_SORTABLE_FIELD_SKU = -651451878;
	LICENSE_TENANT_ASSIGNMENT_SORTABLE_FIELD_SALES_ORDER_NUMBER = -988583176;
	LICENSE_TENANT_ASSIGNMENT_SORTABLE_FIELD_START_DATE = -787429676;
	LICENSE_TENANT_ASSIGNMENT_SORTABLE_FIELD_END_DATE = 1326735083;
	LICENSE_TENANT_ASSIGNMENT_SORTABLE_FIELD_TENANT_ID = 590923987;
}

message LicenseTenantAssignmentPageRequest {
	// Page is the window-index to fetch.
	uint64 page = 1;
	// Page_length is the size of the page. Combined with page, this will return:
	//    start = (page * page_length)
	//    end = start + page_length
	//    return sorted_table[start..end]
	// Page length should be consistent across requests for the same client-context
	uint64 page_length = 2;

	// Sort_field is the field or subfield of the resource to sort by
	LicenseTenantAssignmentSortableField sort_field = 3;

	// Sort_dir is the direction of the sort, ascending or descending
	fmp.SortDirection sort_dir = 4;

	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated LicenseTenantAssignment partial_eq_filter = 5;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetPage, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each LicenseTenantAssignment at end.
	//     * Each LicenseTenantAssignment response is fully-specified (all fields set).
	//   * start: Returns the state of each LicenseTenantAssignment at start, followed by updates until now.
	//     * Each LicenseTenantAssignment response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each LicenseTenantAssignment at start, followed by updates
	//     until end.
	//     * Each LicenseTenantAssignment response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 7;
};

service LicenseTenantAssignmentService {
	rpc GetOne (LicenseTenantAssignmentRequest) returns (LicenseTenantAssignmentResponse);
	rpc GetSome (LicenseTenantAssignmentSomeRequest) returns (stream LicenseTenantAssignmentSomeResponse);
	rpc GetAll (LicenseTenantAssignmentStreamRequest) returns (stream LicenseTenantAssignmentStreamResponse);
	rpc Subscribe (LicenseTenantAssignmentStreamRequest) returns (stream LicenseTenantAssignmentStreamResponse);
	rpc GetMeta (LicenseTenantAssignmentStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (LicenseTenantAssignmentStreamRequest) returns (stream MetaResponse);
	rpc GetAllBatched (LicenseTenantAssignmentBatchedStreamRequest) returns (stream LicenseTenantAssignmentBatchedStreamResponse);
	rpc SubscribeBatched (LicenseTenantAssignmentBatchedStreamRequest) returns (stream LicenseTenantAssignmentBatchedStreamResponse);
	rpc GetPage (LicenseTenantAssignmentPageRequest) returns (stream LicenseTenantAssignmentResponse);
}

message SalesOrderRequest {
	// Key uniquely identifies a SalesOrder instance to retrieve.
	// This value must be populated.
	SalesOrderKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message SalesOrderResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	SalesOrder value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// SalesOrder instance in this response.
	google.protobuf.Timestamp time = 2;
};

message SalesOrderSomeRequest {
	repeated SalesOrderKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message SalesOrderSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	SalesOrder value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	// Time carries the (UTC) timestamp of the last-modification of the
	// SalesOrder instance in this response.
	google.protobuf.Timestamp time = 3;
};

message SalesOrderStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated SalesOrder partial_eq_filter = 1;
	// For each SalesOrder in the list, all populated fields are considered ANDed together
	// as a filtering operation. Similarly, the list itself is ORed such that any individual
	// filter that matches a given SalesOrder is streamed to the user.
	SalesOrderFilter filter = 2;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each SalesOrder at end.
	//     * Each SalesOrder response is fully-specified (all fields set).
	//   * start: Returns the state of each SalesOrder at start, followed by updates until now.
	//     * Each SalesOrder response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each SalesOrder at start, followed by updates
	//     until end.
	//     * Each SalesOrder response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message SalesOrderStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	SalesOrder value = 1;

	// Time holds the timestamp of this SalesOrder's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the SalesOrder value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message SalesOrderBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated SalesOrder partial_eq_filter = 1;
	// For each SalesOrder in the list, all populated fields are considered ANDed together
	// as a filtering operation. Similarly, the list itself is ORed such that any individual
	// filter that matches a given SalesOrder is streamed to the user.
	SalesOrderFilter filter = 2;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each SalesOrder at end.
	//     * Each SalesOrder response is fully-specified (all fields set).
	//   * start: Returns the state of each SalesOrder at start, followed by updates until now.
	//     * Each SalesOrder response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each SalesOrder at start, followed by updates
	//     until end.
	//     * Each SalesOrder response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message SalesOrderBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated SalesOrderStreamResponse responses = 1;
};

service SalesOrderService {
	rpc GetOne (SalesOrderRequest) returns (SalesOrderResponse);
	rpc GetSome (SalesOrderSomeRequest) returns (stream SalesOrderSomeResponse);
	rpc GetAll (SalesOrderStreamRequest) returns (stream SalesOrderStreamResponse);
	rpc Subscribe (SalesOrderStreamRequest) returns (stream SalesOrderStreamResponse);
	rpc GetMeta (SalesOrderStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (SalesOrderStreamRequest) returns (stream MetaResponse);
	rpc GetAllBatched (SalesOrderBatchedStreamRequest) returns (stream SalesOrderBatchedStreamResponse);
	rpc SubscribeBatched (SalesOrderBatchedStreamRequest) returns (stream SalesOrderBatchedStreamResponse);
}
