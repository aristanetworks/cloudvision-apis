// Copyright (c) 2023 Arista Networks, Inc.  All rights reserved.
// Use of this source code is governed by the Apache License 2.0
// that can be found in the COPYING file.

//
// Code generated by boomtown. DO NOT EDIT.
//

syntax = "proto3";

package arista.action.v1;
option go_package = "github.com/aristanetworks/cloudvision-go/api/arista/action.v1;action";

import "arista/action.v1/action.proto";
import "arista/time/time.proto";
import "arista/subscriptions/subscriptions.proto";
import "fmp/deletes.proto";
import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

message MetaResponse {
	// Time holds the timestamp of the last item included in the metadata calculation.
	google.protobuf.Timestamp time = 1;

	// Operation indicates how the value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 2;

	// Count is the number of items present under the conditions of the request.
	google.protobuf.UInt32Value count = 3;
};

message ActionRequest {
	// Key uniquely identifies a Action instance to retrieve.
	// This value must be populated.
	ActionKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ActionResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	Action value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// Action instance in this response.
	google.protobuf.Timestamp time = 2;
};

message ActionSomeRequest {
	repeated ActionKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ActionSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	Action value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	google.protobuf.Timestamp time = 3;
};

message ActionStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated Action partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each Action at end.
	//     * Each Action response is fully-specified (all fields set).
	//   * start: Returns the state of each Action at start, followed by updates until now.
	//     * Each Action response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each Action at start, followed by updates
	//     until end.
	//     * Each Action response at start is fully-specified, but updates until end may
	//       be partial.
	//
	// This field is not allowed in the Subscribe RPC.
	arista.time.TimeBounds time = 3;
};

message ActionStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	Action value = 1;

	// Time holds the timestamp of this Action's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the Action value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

service ActionService {
	rpc GetOne (ActionRequest) returns (ActionResponse);
	rpc GetSome (ActionSomeRequest) returns (stream ActionSomeResponse);
	rpc GetAll (ActionStreamRequest) returns (stream ActionStreamResponse);
	rpc Subscribe (ActionStreamRequest) returns (stream ActionStreamResponse);
	rpc GetMeta (ActionStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (ActionStreamRequest) returns (stream MetaResponse);
}

message ActionArgConfigRequest {
	// Key uniquely identifies a ActionArgConfig instance to retrieve.
	// This value must be populated.
	ActionKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ActionArgConfigResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	ActionArgConfig value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// ActionArgConfig instance in this response.
	google.protobuf.Timestamp time = 2;
};

message ActionArgConfigSomeRequest {
	repeated ActionKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ActionArgConfigSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	ActionArgConfig value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	google.protobuf.Timestamp time = 3;
};

message ActionArgConfigStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated ActionArgConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each ActionArgConfig at end.
	//     * Each ActionArgConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each ActionArgConfig at start, followed by updates until now.
	//     * Each ActionArgConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each ActionArgConfig at start, followed by updates
	//     until end.
	//     * Each ActionArgConfig response at start is fully-specified, but updates until end may
	//       be partial.
	//
	// This field is not allowed in the Subscribe RPC.
	arista.time.TimeBounds time = 3;
};

message ActionArgConfigStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	ActionArgConfig value = 1;

	// Time holds the timestamp of this ActionArgConfig's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the ActionArgConfig value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message ActionArgConfigSetRequest {
	// ActionArgConfig carries the value to set into the datastore.
	// See the documentation on the ActionArgConfig struct for which fields are required.
	ActionArgConfig value = 1;
};

message ActionArgConfigSetResponse {
	// Value carries all the values given in the ActionArgConfigSetRequest as well
	// as any server-generated values.
	ActionArgConfig value = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// creation. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==CreatedAt will include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message ActionArgConfigSetSomeRequest {
	// value contains a list of ActionArgConfig values to write.
	// It is possible to provide more values than can fit within either:
	//     - the maxiumum send size of the client
	//     - the maximum receive size of the server
	// If this error occurs you must reduce the number of values sent.
	// See gRPC "maximum message size" documentation for more information.
	repeated ActionArgConfig values = 1;
};

message ActionArgConfigSetSomeResponse {
	ActionKey key = 1;
	string error = 2;
};

message ActionArgConfigDeleteRequest {
	// Key indicates which ActionArgConfig instance to remove.
	// This field must always be set.
	ActionKey key = 1;
};

message ActionArgConfigDeleteResponse {
	// Key echoes back the key of the deleted ActionArgConfig instance.
	ActionKey key = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// deletion. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==DeletedAt will not include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message ActionArgConfigDeleteSomeRequest {
	// key contains a list of ActionArgConfig keys to delete
	repeated ActionKey keys = 1;
};

// ActionArgConfigDeleteSomeResponse is only sent when there is an error.
message ActionArgConfigDeleteSomeResponse {
	ActionKey key = 1;
	string error = 2;
};

message ActionArgConfigDeleteAllRequest {
	// PartialEqFilter provides a way to server-side filter a DeleteAll.
	// This requires all provided fields to be equal to the response.
	// A filtered DeleteAll will use GetAll with filter to find things to delete.
	repeated ActionArgConfig partial_eq_filter = 1;
};

message ActionArgConfigDeleteAllResponse {
	// This describes the class of delete error.
	// A DeleteAllResponse is only sent when there is an error.
	fmp.DeleteError type = 1;
	// This indicates the error message from the delete failure.
	google.protobuf.StringValue error = 2;
	// This is the key of the ActionArgConfig instance that failed to be deleted.
	ActionKey key = 3;
	// Time indicates the (UTC) timestamp when the key was being deleted.
	google.protobuf.Timestamp time = 4;
};

service ActionArgConfigService {
	rpc GetOne (ActionArgConfigRequest) returns (ActionArgConfigResponse);
	rpc GetSome (ActionArgConfigSomeRequest) returns (stream ActionArgConfigSomeResponse);
	rpc GetAll (ActionArgConfigStreamRequest) returns (stream ActionArgConfigStreamResponse);
	rpc Subscribe (ActionArgConfigStreamRequest) returns (stream ActionArgConfigStreamResponse);
	rpc GetMeta (ActionArgConfigStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (ActionArgConfigStreamRequest) returns (stream MetaResponse);
	rpc Set (ActionArgConfigSetRequest) returns (ActionArgConfigSetResponse);
	rpc SetSome (ActionArgConfigSetSomeRequest) returns (stream ActionArgConfigSetSomeResponse);
	rpc Delete (ActionArgConfigDeleteRequest) returns (ActionArgConfigDeleteResponse);
	rpc DeleteSome (ActionArgConfigDeleteSomeRequest) returns (stream ActionArgConfigDeleteSomeResponse);
	rpc DeleteAll (ActionArgConfigDeleteAllRequest) returns (stream ActionArgConfigDeleteAllResponse);
}

message ActionConfigRequest {
	// Key uniquely identifies a ActionConfig instance to retrieve.
	// This value must be populated.
	ActionKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ActionConfigResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	ActionConfig value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// ActionConfig instance in this response.
	google.protobuf.Timestamp time = 2;
};

message ActionConfigSomeRequest {
	repeated ActionKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ActionConfigSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	ActionConfig value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	google.protobuf.Timestamp time = 3;
};

message ActionConfigStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated ActionConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each ActionConfig at end.
	//     * Each ActionConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each ActionConfig at start, followed by updates until now.
	//     * Each ActionConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each ActionConfig at start, followed by updates
	//     until end.
	//     * Each ActionConfig response at start is fully-specified, but updates until end may
	//       be partial.
	//
	// This field is not allowed in the Subscribe RPC.
	arista.time.TimeBounds time = 3;
};

message ActionConfigStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	ActionConfig value = 1;

	// Time holds the timestamp of this ActionConfig's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the ActionConfig value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message ActionConfigSetRequest {
	// ActionConfig carries the value to set into the datastore.
	// See the documentation on the ActionConfig struct for which fields are required.
	ActionConfig value = 1;
};

message ActionConfigSetResponse {
	// Value carries all the values given in the ActionConfigSetRequest as well
	// as any server-generated values.
	ActionConfig value = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// creation. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==CreatedAt will include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message ActionConfigSetSomeRequest {
	// value contains a list of ActionConfig values to write.
	// It is possible to provide more values than can fit within either:
	//     - the maxiumum send size of the client
	//     - the maximum receive size of the server
	// If this error occurs you must reduce the number of values sent.
	// See gRPC "maximum message size" documentation for more information.
	repeated ActionConfig values = 1;
};

message ActionConfigSetSomeResponse {
	ActionKey key = 1;
	string error = 2;
};

message ActionConfigDeleteRequest {
	// Key indicates which ActionConfig instance to remove.
	// This field must always be set.
	ActionKey key = 1;
};

message ActionConfigDeleteResponse {
	// Key echoes back the key of the deleted ActionConfig instance.
	ActionKey key = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// deletion. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==DeletedAt will not include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message ActionConfigDeleteSomeRequest {
	// key contains a list of ActionConfig keys to delete
	repeated ActionKey keys = 1;
};

// ActionConfigDeleteSomeResponse is only sent when there is an error.
message ActionConfigDeleteSomeResponse {
	ActionKey key = 1;
	string error = 2;
};

message ActionConfigDeleteAllRequest {
	// PartialEqFilter provides a way to server-side filter a DeleteAll.
	// This requires all provided fields to be equal to the response.
	// A filtered DeleteAll will use GetAll with filter to find things to delete.
	repeated ActionConfig partial_eq_filter = 1;
};

message ActionConfigDeleteAllResponse {
	// This describes the class of delete error.
	// A DeleteAllResponse is only sent when there is an error.
	fmp.DeleteError type = 1;
	// This indicates the error message from the delete failure.
	google.protobuf.StringValue error = 2;
	// This is the key of the ActionConfig instance that failed to be deleted.
	ActionKey key = 3;
	// Time indicates the (UTC) timestamp when the key was being deleted.
	google.protobuf.Timestamp time = 4;
};

service ActionConfigService {
	rpc GetOne (ActionConfigRequest) returns (ActionConfigResponse);
	rpc GetSome (ActionConfigSomeRequest) returns (stream ActionConfigSomeResponse);
	rpc GetAll (ActionConfigStreamRequest) returns (stream ActionConfigStreamResponse);
	rpc Subscribe (ActionConfigStreamRequest) returns (stream ActionConfigStreamResponse);
	rpc GetMeta (ActionConfigStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (ActionConfigStreamRequest) returns (stream MetaResponse);
	rpc Set (ActionConfigSetRequest) returns (ActionConfigSetResponse);
	rpc SetSome (ActionConfigSetSomeRequest) returns (stream ActionConfigSetSomeResponse);
	rpc Delete (ActionConfigDeleteRequest) returns (ActionConfigDeleteResponse);
	rpc DeleteSome (ActionConfigDeleteSomeRequest) returns (stream ActionConfigDeleteSomeResponse);
	rpc DeleteAll (ActionConfigDeleteAllRequest) returns (stream ActionConfigDeleteAllResponse);
}

message ActionRunRequest {
	// Key uniquely identifies a ActionRun instance to retrieve.
	// This value must be populated.
	ActionRunKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ActionRunResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	ActionRun value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// ActionRun instance in this response.
	google.protobuf.Timestamp time = 2;
};

message ActionRunSomeRequest {
	repeated ActionRunKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ActionRunSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	ActionRun value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	google.protobuf.Timestamp time = 3;
};

message ActionRunStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated ActionRun partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each ActionRun at end.
	//     * Each ActionRun response is fully-specified (all fields set).
	//   * start: Returns the state of each ActionRun at start, followed by updates until now.
	//     * Each ActionRun response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each ActionRun at start, followed by updates
	//     until end.
	//     * Each ActionRun response at start is fully-specified, but updates until end may
	//       be partial.
	//
	// This field is not allowed in the Subscribe RPC.
	arista.time.TimeBounds time = 3;
};

message ActionRunStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	ActionRun value = 1;

	// Time holds the timestamp of this ActionRun's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the ActionRun value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

service ActionRunService {
	rpc GetOne (ActionRunRequest) returns (ActionRunResponse);
	rpc GetSome (ActionRunSomeRequest) returns (stream ActionRunSomeResponse);
	rpc GetAll (ActionRunStreamRequest) returns (stream ActionRunStreamResponse);
	rpc Subscribe (ActionRunStreamRequest) returns (stream ActionRunStreamResponse);
	rpc GetMeta (ActionRunStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (ActionRunStreamRequest) returns (stream MetaResponse);
}

message ActionRunConfigRequest {
	// Key uniquely identifies a ActionRunConfig instance to retrieve.
	// This value must be populated.
	ActionRunKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ActionRunConfigResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	ActionRunConfig value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// ActionRunConfig instance in this response.
	google.protobuf.Timestamp time = 2;
};

message ActionRunConfigSomeRequest {
	repeated ActionRunKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ActionRunConfigSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	ActionRunConfig value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	google.protobuf.Timestamp time = 3;
};

message ActionRunConfigStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated ActionRunConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each ActionRunConfig at end.
	//     * Each ActionRunConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each ActionRunConfig at start, followed by updates until now.
	//     * Each ActionRunConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each ActionRunConfig at start, followed by updates
	//     until end.
	//     * Each ActionRunConfig response at start is fully-specified, but updates until end may
	//       be partial.
	//
	// This field is not allowed in the Subscribe RPC.
	arista.time.TimeBounds time = 3;
};

message ActionRunConfigStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	ActionRunConfig value = 1;

	// Time holds the timestamp of this ActionRunConfig's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the ActionRunConfig value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message ActionRunConfigSetRequest {
	// ActionRunConfig carries the value to set into the datastore.
	// See the documentation on the ActionRunConfig struct for which fields are required.
	ActionRunConfig value = 1;
};

message ActionRunConfigSetResponse {
	// Value carries all the values given in the ActionRunConfigSetRequest as well
	// as any server-generated values.
	ActionRunConfig value = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// creation. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==CreatedAt will include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message ActionRunConfigSetSomeRequest {
	// value contains a list of ActionRunConfig values to write.
	// It is possible to provide more values than can fit within either:
	//     - the maxiumum send size of the client
	//     - the maximum receive size of the server
	// If this error occurs you must reduce the number of values sent.
	// See gRPC "maximum message size" documentation for more information.
	repeated ActionRunConfig values = 1;
};

message ActionRunConfigSetSomeResponse {
	ActionRunKey key = 1;
	string error = 2;
};

message ActionRunConfigDeleteRequest {
	// Key indicates which ActionRunConfig instance to remove.
	// This field must always be set.
	ActionRunKey key = 1;
};

message ActionRunConfigDeleteResponse {
	// Key echoes back the key of the deleted ActionRunConfig instance.
	ActionRunKey key = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// deletion. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==DeletedAt will not include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message ActionRunConfigDeleteSomeRequest {
	// key contains a list of ActionRunConfig keys to delete
	repeated ActionRunKey keys = 1;
};

// ActionRunConfigDeleteSomeResponse is only sent when there is an error.
message ActionRunConfigDeleteSomeResponse {
	ActionRunKey key = 1;
	string error = 2;
};

message ActionRunConfigDeleteAllRequest {
	// PartialEqFilter provides a way to server-side filter a DeleteAll.
	// This requires all provided fields to be equal to the response.
	// A filtered DeleteAll will use GetAll with filter to find things to delete.
	repeated ActionRunConfig partial_eq_filter = 1;
};

message ActionRunConfigDeleteAllResponse {
	// This describes the class of delete error.
	// A DeleteAllResponse is only sent when there is an error.
	fmp.DeleteError type = 1;
	// This indicates the error message from the delete failure.
	google.protobuf.StringValue error = 2;
	// This is the key of the ActionRunConfig instance that failed to be deleted.
	ActionRunKey key = 3;
	// Time indicates the (UTC) timestamp when the key was being deleted.
	google.protobuf.Timestamp time = 4;
};

service ActionRunConfigService {
	rpc GetOne (ActionRunConfigRequest) returns (ActionRunConfigResponse);
	rpc GetSome (ActionRunConfigSomeRequest) returns (stream ActionRunConfigSomeResponse);
	rpc GetAll (ActionRunConfigStreamRequest) returns (stream ActionRunConfigStreamResponse);
	rpc Subscribe (ActionRunConfigStreamRequest) returns (stream ActionRunConfigStreamResponse);
	rpc GetMeta (ActionRunConfigStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (ActionRunConfigStreamRequest) returns (stream MetaResponse);
	rpc Set (ActionRunConfigSetRequest) returns (ActionRunConfigSetResponse);
	rpc SetSome (ActionRunConfigSetSomeRequest) returns (stream ActionRunConfigSetSomeResponse);
	rpc Delete (ActionRunConfigDeleteRequest) returns (ActionRunConfigDeleteResponse);
	rpc DeleteSome (ActionRunConfigDeleteSomeRequest) returns (stream ActionRunConfigDeleteSomeResponse);
	rpc DeleteAll (ActionRunConfigDeleteAllRequest) returns (stream ActionRunConfigDeleteAllResponse);
}
