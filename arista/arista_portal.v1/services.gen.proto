// Copyright (c) 2025 Arista Networks, Inc.  All rights reserved.
// Use of this source code is governed by the Apache License 2.0
// that can be found in the COPYING file.

// ------------------------------------------------------------------------------------------------
//
// The Arista Portal APIs provide CloudVision with programmatic access to resources
// available on the arista.com site.
//
// Resources include:
//      * AlertBase updates (AlertBase.json) and
//      * device software downloads.
//
// APIToken & APITokenConfig are the resource API models which facilitate users to set,
// delete and check the presence of a valid Arista Portal API token.
//
// On-premises deployments may require a proxy to allow reachability between CloudVision
// and arista.com depending on the deployment environment. APIProxy & APIProxyConfig are the
// resource API models which can be used to configure CloudVision with a proxy URL where relevant.
//
// ------------------------------------------------------------------------------------------------

//
// Code generated by boomtown. DO NOT EDIT.
//

syntax = "proto3";

package arista.arista_portal.v1;
option go_package = "github.com/aristanetworks/cloudvision-go/api/arista/arista_portal.v1;arista_portal";

option java_package = "com.arista.arista_portal.v1";
option java_outer_classname = "AristaPortalServices";
option java_multiple_files = true;

import "arista/arista_portal.v1/arista_portal.proto";
import "arista/time/time.proto";
import "arista/subscriptions/subscriptions.proto";
import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

message MetaResponse {
	// Time holds the timestamp of the last item included in the metadata calculation.
	google.protobuf.Timestamp time = 1;

	// Operation indicates how the value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 2;

	// Count is the number of items present under the conditions of the request.
	google.protobuf.UInt32Value count = 3;
};

message APIProxyRequest {

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message APIProxyResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	APIProxy value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// APIProxy instance in this response.
	google.protobuf.Timestamp time = 2;
};

message APIProxyStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated APIProxy partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each APIProxy at end.
	//     * Each APIProxy response is fully-specified (all fields set).
	//   * start: Returns the state of each APIProxy at start, followed by updates until now.
	//     * Each APIProxy response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each APIProxy at start, followed by updates
	//     until end.
	//     * Each APIProxy response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message APIProxyStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	APIProxy value = 1;

	// Time holds the timestamp of this APIProxy's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the APIProxy value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

service APIProxyService {
	rpc GetOne (APIProxyRequest) returns (APIProxyResponse);
	rpc GetAll (APIProxyStreamRequest) returns (stream APIProxyStreamResponse);
	rpc Subscribe (APIProxyStreamRequest) returns (stream APIProxyStreamResponse);
	rpc SubscribeMeta (APIProxyStreamRequest) returns (stream MetaResponse);
}

message APIProxyConfigRequest {

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message APIProxyConfigResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	APIProxyConfig value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// APIProxyConfig instance in this response.
	google.protobuf.Timestamp time = 2;
};

message APIProxyConfigStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated APIProxyConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each APIProxyConfig at end.
	//     * Each APIProxyConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each APIProxyConfig at start, followed by updates until now.
	//     * Each APIProxyConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each APIProxyConfig at start, followed by updates
	//     until end.
	//     * Each APIProxyConfig response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message APIProxyConfigStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	APIProxyConfig value = 1;

	// Time holds the timestamp of this APIProxyConfig's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the APIProxyConfig value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message APIProxyConfigSetRequest {
	// APIProxyConfig carries the value to set into the datastore.
	// See the documentation on the APIProxyConfig struct for which fields are required.
	APIProxyConfig value = 1;
};

message APIProxyConfigSetResponse {
	// Value carries all the values given in the APIProxyConfigSetRequest as well
	// as any server-generated values.
	APIProxyConfig value = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// creation. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==CreatedAt will include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message APIProxyConfigDeleteRequest {
};

message APIProxyConfigDeleteResponse {
	// Time indicates the (UTC) timestamp at which the system recognizes the
	// deletion. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==DeletedAt will not include this instance.
	//
	google.protobuf.Timestamp time = 1;
};

service APIProxyConfigService {
	rpc GetOne (APIProxyConfigRequest) returns (APIProxyConfigResponse);
	rpc GetAll (APIProxyConfigStreamRequest) returns (stream APIProxyConfigStreamResponse);
	rpc Subscribe (APIProxyConfigStreamRequest) returns (stream APIProxyConfigStreamResponse);
	rpc SubscribeMeta (APIProxyConfigStreamRequest) returns (stream MetaResponse);
	rpc Set (APIProxyConfigSetRequest) returns (APIProxyConfigSetResponse);
	rpc Delete (APIProxyConfigDeleteRequest) returns (APIProxyConfigDeleteResponse);
	
}

message APITokenRequest {

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message APITokenResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	APIToken value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// APIToken instance in this response.
	google.protobuf.Timestamp time = 2;
};

message APITokenStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated APIToken partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each APIToken at end.
	//     * Each APIToken response is fully-specified (all fields set).
	//   * start: Returns the state of each APIToken at start, followed by updates until now.
	//     * Each APIToken response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each APIToken at start, followed by updates
	//     until end.
	//     * Each APIToken response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message APITokenStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	APIToken value = 1;

	// Time holds the timestamp of this APIToken's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the APIToken value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

service APITokenService {
	rpc GetOne (APITokenRequest) returns (APITokenResponse);
	rpc GetAll (APITokenStreamRequest) returns (stream APITokenStreamResponse);
	rpc Subscribe (APITokenStreamRequest) returns (stream APITokenStreamResponse);
	rpc SubscribeMeta (APITokenStreamRequest) returns (stream MetaResponse);
}

message APITokenConfigRequest {

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message APITokenConfigResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	APITokenConfig value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// APITokenConfig instance in this response.
	google.protobuf.Timestamp time = 2;
};

message APITokenConfigStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated APITokenConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each APITokenConfig at end.
	//     * Each APITokenConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each APITokenConfig at start, followed by updates until now.
	//     * Each APITokenConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each APITokenConfig at start, followed by updates
	//     until end.
	//     * Each APITokenConfig response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message APITokenConfigStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	APITokenConfig value = 1;

	// Time holds the timestamp of this APITokenConfig's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the APITokenConfig value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message APITokenConfigSetRequest {
	// APITokenConfig carries the value to set into the datastore.
	// See the documentation on the APITokenConfig struct for which fields are required.
	APITokenConfig value = 1;
};

message APITokenConfigSetResponse {
	// Value carries all the values given in the APITokenConfigSetRequest as well
	// as any server-generated values.
	APITokenConfig value = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// creation. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==CreatedAt will include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message APITokenConfigDeleteRequest {
};

message APITokenConfigDeleteResponse {
	// Time indicates the (UTC) timestamp at which the system recognizes the
	// deletion. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==DeletedAt will not include this instance.
	//
	google.protobuf.Timestamp time = 1;
};

service APITokenConfigService {
	rpc GetOne (APITokenConfigRequest) returns (APITokenConfigResponse);
	rpc GetAll (APITokenConfigStreamRequest) returns (stream APITokenConfigStreamResponse);
	rpc Subscribe (APITokenConfigStreamRequest) returns (stream APITokenConfigStreamResponse);
	rpc SubscribeMeta (APITokenConfigStreamRequest) returns (stream MetaResponse);
	rpc Set (APITokenConfigSetRequest) returns (APITokenConfigSetResponse);
	rpc Delete (APITokenConfigDeleteRequest) returns (APITokenConfigDeleteResponse);
	
}
