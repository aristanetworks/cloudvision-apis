// Copyright (c) 2023 Arista Networks, Inc.  All rights reserved.
// Use of this source code is governed by the Apache License 2.0
// that can be found in the COPYING file.

// EOS images, Streaming Agent, and other extensions in CloudVision are managed using this
// resource API. Images and extensions can be added, deleted and updated.
//
// New software is added to CloudVision either by uploading files directly from your computer using
// the Software Upload REST API or by downloading them from Arista's Software Download site using
// this resource API. To upload software using the Software Upload REST API in CloudVision, navigate
// to Settings > Developer Tools > REST API Explorer > softwareRepository.
//
// No matter how the image or extension is added, you can use the resource API to update or delete
// it.

//
// Code generated by boomtown. DO NOT EDIT.
//

syntax = "proto3";

package arista.softwaremanagement.v1;
option go_package = "github.com/aristanetworks/cloudvision-go/api/arista/softwaremanagement.v1;softwaremanagement";

option java_package = "com.arista.softwaremanagement.v1";
option java_outer_classname = "SoftwaremanagementServices";
option java_multiple_files = true;

import "arista/softwaremanagement.v1/softwaremanagement.proto";
import "arista/time/time.proto";
import "arista/subscriptions/subscriptions.proto";
import "fmp/deletes.proto";
import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

message MetaResponse {
	// Time holds the timestamp of the last item included in the metadata calculation.
	google.protobuf.Timestamp time = 1;

	// Operation indicates how the value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 2;

	// Count is the number of items present under the conditions of the request.
	google.protobuf.UInt32Value count = 3;
};

message ReleasesRequest {

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ReleasesResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	Releases value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// Releases instance in this response.
	google.protobuf.Timestamp time = 2;
};

message ReleasesStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated Releases partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each Releases at end.
	//     * Each Releases response is fully-specified (all fields set).
	//   * start: Returns the state of each Releases at start, followed by updates until now.
	//     * Each Releases response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each Releases at start, followed by updates
	//     until end.
	//     * Each Releases response at start is fully-specified, but updates until end may
	//       be partial.
	//
	// This field is not allowed in the Subscribe RPC.
	arista.time.TimeBounds time = 3;
};

message ReleasesStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	Releases value = 1;

	// Time holds the timestamp of this Releases's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the Releases value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

service ReleasesService {
	rpc GetOne (ReleasesRequest) returns (ReleasesResponse);
	rpc GetAll (ReleasesStreamRequest) returns (stream ReleasesStreamResponse);
	rpc Subscribe (ReleasesStreamRequest) returns (stream ReleasesStreamResponse);
	rpc SubscribeMeta (ReleasesStreamRequest) returns (stream MetaResponse);
}

message RepositoryRequest {
	// Key uniquely identifies a Repository instance to retrieve.
	// This value must be populated.
	RepositoryKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message RepositoryResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	Repository value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// Repository instance in this response.
	google.protobuf.Timestamp time = 2;
};

message RepositorySomeRequest {
	repeated RepositoryKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message RepositorySomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	Repository value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	google.protobuf.Timestamp time = 3;
};

message RepositoryStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated Repository partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each Repository at end.
	//     * Each Repository response is fully-specified (all fields set).
	//   * start: Returns the state of each Repository at start, followed by updates until now.
	//     * Each Repository response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each Repository at start, followed by updates
	//     until end.
	//     * Each Repository response at start is fully-specified, but updates until end may
	//       be partial.
	//
	// This field is not allowed in the Subscribe RPC.
	arista.time.TimeBounds time = 3;
};

message RepositoryStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	Repository value = 1;

	// Time holds the timestamp of this Repository's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the Repository value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message RepositoryBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated Repository partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each Repository at end.
	//     * Each Repository response is fully-specified (all fields set).
	//   * start: Returns the state of each Repository at start, followed by updates until now.
	//     * Each Repository response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each Repository at start, followed by updates
	//     until end.
	//     * Each Repository response at start is fully-specified, but updates until end may
	//       be partial.
	//
	// This field is not allowed in the Subscribe RPC.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message RepositoryBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and 
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated RepositoryStreamResponse responses = 1;
};

service RepositoryService {
	rpc GetOne (RepositoryRequest) returns (RepositoryResponse);
	rpc GetSome (RepositorySomeRequest) returns (stream RepositorySomeResponse);
	rpc GetAll (RepositoryStreamRequest) returns (stream RepositoryStreamResponse);
	rpc Subscribe (RepositoryStreamRequest) returns (stream RepositoryStreamResponse);
	rpc GetMeta (RepositoryStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (RepositoryStreamRequest) returns (stream MetaResponse);
	rpc GetAllBatched (RepositoryBatchedStreamRequest) returns (stream RepositoryBatchedStreamResponse);
	rpc SubscribeBatched (RepositoryBatchedStreamRequest) returns (stream RepositoryBatchedStreamResponse);
}

message RepositoryConfigRequest {
	// Key uniquely identifies a RepositoryConfig instance to retrieve.
	// This value must be populated.
	RepositoryKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message RepositoryConfigResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	RepositoryConfig value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// RepositoryConfig instance in this response.
	google.protobuf.Timestamp time = 2;
};

message RepositoryConfigSomeRequest {
	repeated RepositoryKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message RepositoryConfigSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	RepositoryConfig value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	google.protobuf.Timestamp time = 3;
};

message RepositoryConfigStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated RepositoryConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each RepositoryConfig at end.
	//     * Each RepositoryConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each RepositoryConfig at start, followed by updates until now.
	//     * Each RepositoryConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each RepositoryConfig at start, followed by updates
	//     until end.
	//     * Each RepositoryConfig response at start is fully-specified, but updates until end may
	//       be partial.
	//
	// This field is not allowed in the Subscribe RPC.
	arista.time.TimeBounds time = 3;
};

message RepositoryConfigStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	RepositoryConfig value = 1;

	// Time holds the timestamp of this RepositoryConfig's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the RepositoryConfig value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message RepositoryConfigBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated RepositoryConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each RepositoryConfig at end.
	//     * Each RepositoryConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each RepositoryConfig at start, followed by updates until now.
	//     * Each RepositoryConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each RepositoryConfig at start, followed by updates
	//     until end.
	//     * Each RepositoryConfig response at start is fully-specified, but updates until end may
	//       be partial.
	//
	// This field is not allowed in the Subscribe RPC.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message RepositoryConfigBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and 
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated RepositoryConfigStreamResponse responses = 1;
};

message RepositoryConfigSetRequest {
	// RepositoryConfig carries the value to set into the datastore.
	// See the documentation on the RepositoryConfig struct for which fields are required.
	RepositoryConfig value = 1;
};

message RepositoryConfigSetResponse {
	// Value carries all the values given in the RepositoryConfigSetRequest as well
	// as any server-generated values.
	RepositoryConfig value = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// creation. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==CreatedAt will include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message RepositoryConfigSetSomeRequest {
	// value contains a list of RepositoryConfig values to write.
	// It is possible to provide more values than can fit within either:
	//     - the maxiumum send size of the client
	//     - the maximum receive size of the server
	// If this error occurs you must reduce the number of values sent.
	// See gRPC "maximum message size" documentation for more information.
	repeated RepositoryConfig values = 1;
};

message RepositoryConfigSetSomeResponse {
	RepositoryKey key = 1;
	string error = 2;
};

message RepositoryConfigDeleteRequest {
	// Key indicates which RepositoryConfig instance to remove.
	// This field must always be set.
	RepositoryKey key = 1;
};

message RepositoryConfigDeleteResponse {
	// Key echoes back the key of the deleted RepositoryConfig instance.
	RepositoryKey key = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// deletion. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==DeletedAt will not include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message RepositoryConfigDeleteSomeRequest {
	// key contains a list of RepositoryConfig keys to delete
	repeated RepositoryKey keys = 1;
};

// RepositoryConfigDeleteSomeResponse is only sent when there is an error.
message RepositoryConfigDeleteSomeResponse {
	RepositoryKey key = 1;
	string error = 2;
};

message RepositoryConfigDeleteAllRequest {
	// PartialEqFilter provides a way to server-side filter a DeleteAll.
	// This requires all provided fields to be equal to the response.
	// A filtered DeleteAll will use GetAll with filter to find things to delete.
	repeated RepositoryConfig partial_eq_filter = 1;
};

message RepositoryConfigDeleteAllResponse {
	// This describes the class of delete error.
	// A DeleteAllResponse is only sent when there is an error.
	fmp.DeleteError type = 1;
	// This indicates the error message from the delete failure.
	google.protobuf.StringValue error = 2;
	// This is the key of the RepositoryConfig instance that failed to be deleted.
	RepositoryKey key = 3;
	// Time indicates the (UTC) timestamp when the key was being deleted.
	google.protobuf.Timestamp time = 4;
};

service RepositoryConfigService {
	rpc GetOne (RepositoryConfigRequest) returns (RepositoryConfigResponse);
	rpc GetSome (RepositoryConfigSomeRequest) returns (stream RepositoryConfigSomeResponse);
	rpc GetAll (RepositoryConfigStreamRequest) returns (stream RepositoryConfigStreamResponse);
	rpc Subscribe (RepositoryConfigStreamRequest) returns (stream RepositoryConfigStreamResponse);
	rpc GetMeta (RepositoryConfigStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (RepositoryConfigStreamRequest) returns (stream MetaResponse);
	rpc Set (RepositoryConfigSetRequest) returns (RepositoryConfigSetResponse);
	rpc SetSome (RepositoryConfigSetSomeRequest) returns (stream RepositoryConfigSetSomeResponse);
	rpc Delete (RepositoryConfigDeleteRequest) returns (RepositoryConfigDeleteResponse);
	rpc DeleteSome (RepositoryConfigDeleteSomeRequest) returns (stream RepositoryConfigDeleteSomeResponse);
	rpc DeleteAll (RepositoryConfigDeleteAllRequest) returns (stream RepositoryConfigDeleteAllResponse);
	rpc GetAllBatched (RepositoryConfigBatchedStreamRequest) returns (stream RepositoryConfigBatchedStreamResponse);
	rpc SubscribeBatched (RepositoryConfigBatchedStreamRequest) returns (stream RepositoryConfigBatchedStreamResponse);
}
