// Copyright (c) 2023 Arista Networks, Inc.  All rights reserved.
// Use of this source code is governed by the Apache License 2.0
// that can be found in the COPYING file.

// These APIs are part of License management solution, the basic workflow  -
// 1. Sales order placed by customer.
// 2. Generate list of purchased licenses from sales and invoice data.
// 3. Assign the device to a purchased license.
// 4. Procure license file for the assigned device license.
// 5. Install generated license file from procurement to assigned device.

//
// Code generated by boomtown. DO NOT EDIT.
//

syntax = "proto3";

package arista.license.v1;
option go_package = "github.com/aristanetworks/cloudvision-go/api/arista/license.v1;license";

option java_package = "com.arista.license.v1";
option java_outer_classname = "LicenseServices";
option java_multiple_files = true;

import "arista/license.v1/license.proto";
import "arista/time/time.proto";
import "arista/subscriptions/subscriptions.proto";
import "fmp/deletes.proto";
import "google/protobuf/timestamp.proto";
import "google/protobuf/wrappers.proto";

message MetaResponse {
	// Time holds the timestamp of the last item included in the metadata calculation.
	google.protobuf.Timestamp time = 1;

	// Operation indicates how the value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 2;

	// Count is the number of items present under the conditions of the request.
	google.protobuf.UInt32Value count = 3;
};

message ApplicableDevicesRequest {
	// Key uniquely identifies a ApplicableDevices instance to retrieve.
	// This value must be populated.
	ApplicableDevicesKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ApplicableDevicesResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	ApplicableDevices value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// ApplicableDevices instance in this response.
	google.protobuf.Timestamp time = 2;
};

message ApplicableDevicesSomeRequest {
	repeated ApplicableDevicesKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ApplicableDevicesSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	ApplicableDevices value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	// Time carries the (UTC) timestamp of the last-modification of the
	// ApplicableDevices instance in this response.
	google.protobuf.Timestamp time = 3;
};

message ApplicableDevicesStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated ApplicableDevices partial_eq_filter = 1;
	// For each ApplicableDevices in the list, all populated fields are considered ANDed together
	// as a filtering operation. Similarly, the list itself is ORed such that any individual
	// filter that matches a given ApplicableDevices is streamed to the user.
	Filter filter = 2;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each ApplicableDevices at end.
	//     * Each ApplicableDevices response is fully-specified (all fields set).
	//   * start: Returns the state of each ApplicableDevices at start, followed by updates until now.
	//     * Each ApplicableDevices response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each ApplicableDevices at start, followed by updates
	//     until end.
	//     * Each ApplicableDevices response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message ApplicableDevicesStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	ApplicableDevices value = 1;

	// Time holds the timestamp of this ApplicableDevices's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the ApplicableDevices value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message ApplicableDevicesBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated ApplicableDevices partial_eq_filter = 1;
	// For each ApplicableDevices in the list, all populated fields are considered ANDed together
	// as a filtering operation. Similarly, the list itself is ORed such that any individual
	// filter that matches a given ApplicableDevices is streamed to the user.
	Filter filter = 2;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each ApplicableDevices at end.
	//     * Each ApplicableDevices response is fully-specified (all fields set).
	//   * start: Returns the state of each ApplicableDevices at start, followed by updates until now.
	//     * Each ApplicableDevices response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each ApplicableDevices at start, followed by updates
	//     until end.
	//     * Each ApplicableDevices response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message ApplicableDevicesBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated ApplicableDevicesStreamResponse responses = 1;
};

service ApplicableDevicesService {
	rpc GetOne (ApplicableDevicesRequest) returns (ApplicableDevicesResponse);
	rpc GetSome (ApplicableDevicesSomeRequest) returns (stream ApplicableDevicesSomeResponse);
	rpc GetAll (ApplicableDevicesStreamRequest) returns (stream ApplicableDevicesStreamResponse);
	rpc Subscribe (ApplicableDevicesStreamRequest) returns (stream ApplicableDevicesStreamResponse);
	rpc GetMeta (ApplicableDevicesStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (ApplicableDevicesStreamRequest) returns (stream MetaResponse);
	rpc GetAllBatched (ApplicableDevicesBatchedStreamRequest) returns (stream ApplicableDevicesBatchedStreamResponse);
	rpc SubscribeBatched (ApplicableDevicesBatchedStreamRequest) returns (stream ApplicableDevicesBatchedStreamResponse);
}

message ApplicableLicenseBundlesRequest {
	// Key uniquely identifies a ApplicableLicenseBundles instance to retrieve.
	// This value must be populated.
	ApplicableLicenseBundlesKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ApplicableLicenseBundlesResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	ApplicableLicenseBundles value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// ApplicableLicenseBundles instance in this response.
	google.protobuf.Timestamp time = 2;
};

message ApplicableLicenseBundlesSomeRequest {
	repeated ApplicableLicenseBundlesKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message ApplicableLicenseBundlesSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	ApplicableLicenseBundles value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	// Time carries the (UTC) timestamp of the last-modification of the
	// ApplicableLicenseBundles instance in this response.
	google.protobuf.Timestamp time = 3;
};

message ApplicableLicenseBundlesStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated ApplicableLicenseBundles partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each ApplicableLicenseBundles at end.
	//     * Each ApplicableLicenseBundles response is fully-specified (all fields set).
	//   * start: Returns the state of each ApplicableLicenseBundles at start, followed by updates until now.
	//     * Each ApplicableLicenseBundles response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each ApplicableLicenseBundles at start, followed by updates
	//     until end.
	//     * Each ApplicableLicenseBundles response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message ApplicableLicenseBundlesStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	ApplicableLicenseBundles value = 1;

	// Time holds the timestamp of this ApplicableLicenseBundles's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the ApplicableLicenseBundles value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message ApplicableLicenseBundlesBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated ApplicableLicenseBundles partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each ApplicableLicenseBundles at end.
	//     * Each ApplicableLicenseBundles response is fully-specified (all fields set).
	//   * start: Returns the state of each ApplicableLicenseBundles at start, followed by updates until now.
	//     * Each ApplicableLicenseBundles response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each ApplicableLicenseBundles at start, followed by updates
	//     until end.
	//     * Each ApplicableLicenseBundles response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message ApplicableLicenseBundlesBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated ApplicableLicenseBundlesStreamResponse responses = 1;
};

service ApplicableLicenseBundlesService {
	rpc GetOne (ApplicableLicenseBundlesRequest) returns (ApplicableLicenseBundlesResponse);
	rpc GetSome (ApplicableLicenseBundlesSomeRequest) returns (stream ApplicableLicenseBundlesSomeResponse);
	rpc GetAll (ApplicableLicenseBundlesStreamRequest) returns (stream ApplicableLicenseBundlesStreamResponse);
	rpc Subscribe (ApplicableLicenseBundlesStreamRequest) returns (stream ApplicableLicenseBundlesStreamResponse);
	rpc GetMeta (ApplicableLicenseBundlesStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (ApplicableLicenseBundlesStreamRequest) returns (stream MetaResponse);
	rpc GetAllBatched (ApplicableLicenseBundlesBatchedStreamRequest) returns (stream ApplicableLicenseBundlesBatchedStreamResponse);
	rpc SubscribeBatched (ApplicableLicenseBundlesBatchedStreamRequest) returns (stream ApplicableLicenseBundlesBatchedStreamResponse);
}

message LicenseAssignmentRequest {
	// Key uniquely identifies a LicenseAssignment instance to retrieve.
	// This value must be populated.
	LicenseAssignmentKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message LicenseAssignmentResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	LicenseAssignment value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// LicenseAssignment instance in this response.
	google.protobuf.Timestamp time = 2;
};

message LicenseAssignmentSomeRequest {
	repeated LicenseAssignmentKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message LicenseAssignmentSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	LicenseAssignment value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	// Time carries the (UTC) timestamp of the last-modification of the
	// LicenseAssignment instance in this response.
	google.protobuf.Timestamp time = 3;
};

message LicenseAssignmentStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated LicenseAssignment partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each LicenseAssignment at end.
	//     * Each LicenseAssignment response is fully-specified (all fields set).
	//   * start: Returns the state of each LicenseAssignment at start, followed by updates until now.
	//     * Each LicenseAssignment response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each LicenseAssignment at start, followed by updates
	//     until end.
	//     * Each LicenseAssignment response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message LicenseAssignmentStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	LicenseAssignment value = 1;

	// Time holds the timestamp of this LicenseAssignment's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the LicenseAssignment value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message LicenseAssignmentBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated LicenseAssignment partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each LicenseAssignment at end.
	//     * Each LicenseAssignment response is fully-specified (all fields set).
	//   * start: Returns the state of each LicenseAssignment at start, followed by updates until now.
	//     * Each LicenseAssignment response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each LicenseAssignment at start, followed by updates
	//     until end.
	//     * Each LicenseAssignment response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message LicenseAssignmentBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated LicenseAssignmentStreamResponse responses = 1;
};

service LicenseAssignmentService {
	rpc GetOne (LicenseAssignmentRequest) returns (LicenseAssignmentResponse);
	rpc GetSome (LicenseAssignmentSomeRequest) returns (stream LicenseAssignmentSomeResponse);
	rpc GetAll (LicenseAssignmentStreamRequest) returns (stream LicenseAssignmentStreamResponse);
	rpc Subscribe (LicenseAssignmentStreamRequest) returns (stream LicenseAssignmentStreamResponse);
	rpc GetMeta (LicenseAssignmentStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (LicenseAssignmentStreamRequest) returns (stream MetaResponse);
	rpc GetAllBatched (LicenseAssignmentBatchedStreamRequest) returns (stream LicenseAssignmentBatchedStreamResponse);
	rpc SubscribeBatched (LicenseAssignmentBatchedStreamRequest) returns (stream LicenseAssignmentBatchedStreamResponse);
}

message LicenseAssignmentConfigRequest {
	// Key uniquely identifies a LicenseAssignmentConfig instance to retrieve.
	// This value must be populated.
	LicenseAssignmentKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message LicenseAssignmentConfigResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	LicenseAssignmentConfig value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// LicenseAssignmentConfig instance in this response.
	google.protobuf.Timestamp time = 2;
};

message LicenseAssignmentConfigSomeRequest {
	repeated LicenseAssignmentKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message LicenseAssignmentConfigSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	LicenseAssignmentConfig value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	// Time carries the (UTC) timestamp of the last-modification of the
	// LicenseAssignmentConfig instance in this response.
	google.protobuf.Timestamp time = 3;
};

message LicenseAssignmentConfigStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated LicenseAssignmentConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each LicenseAssignmentConfig at end.
	//     * Each LicenseAssignmentConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each LicenseAssignmentConfig at start, followed by updates until now.
	//     * Each LicenseAssignmentConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each LicenseAssignmentConfig at start, followed by updates
	//     until end.
	//     * Each LicenseAssignmentConfig response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message LicenseAssignmentConfigStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	LicenseAssignmentConfig value = 1;

	// Time holds the timestamp of this LicenseAssignmentConfig's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the LicenseAssignmentConfig value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message LicenseAssignmentConfigBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated LicenseAssignmentConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each LicenseAssignmentConfig at end.
	//     * Each LicenseAssignmentConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each LicenseAssignmentConfig at start, followed by updates until now.
	//     * Each LicenseAssignmentConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each LicenseAssignmentConfig at start, followed by updates
	//     until end.
	//     * Each LicenseAssignmentConfig response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message LicenseAssignmentConfigBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated LicenseAssignmentConfigStreamResponse responses = 1;
};

message LicenseAssignmentConfigSetRequest {
	// LicenseAssignmentConfig carries the value to set into the datastore.
	// See the documentation on the LicenseAssignmentConfig struct for which fields are required.
	LicenseAssignmentConfig value = 1;
};

message LicenseAssignmentConfigSetResponse {
	// Value carries all the values given in the LicenseAssignmentConfigSetRequest as well
	// as any server-generated values.
	LicenseAssignmentConfig value = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// creation. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==CreatedAt will include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message LicenseAssignmentConfigSetSomeRequest {
	// value contains a list of LicenseAssignmentConfig values to write.
	// It is possible to provide more values than can fit within either:
	//     - the maxiumum send size of the client
	//     - the maximum receive size of the server
	// If this error occurs you must reduce the number of values sent.
	// See gRPC "maximum message size" documentation for more information.
	repeated LicenseAssignmentConfig values = 1;
};

message LicenseAssignmentConfigSetSomeResponse {
	LicenseAssignmentKey key = 1;
	string error = 2;
};

message LicenseAssignmentConfigDeleteRequest {
	// Key indicates which LicenseAssignmentConfig instance to remove.
	// This field must always be set.
	LicenseAssignmentKey key = 1;
};

message LicenseAssignmentConfigDeleteResponse {
	// Key echoes back the key of the deleted LicenseAssignmentConfig instance.
	LicenseAssignmentKey key = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// deletion. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==DeletedAt will not include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message LicenseAssignmentConfigDeleteSomeRequest {
	// key contains a list of LicenseAssignmentConfig keys to delete
	repeated LicenseAssignmentKey keys = 1;
};

// LicenseAssignmentConfigDeleteSomeResponse is only sent when there is an error.
message LicenseAssignmentConfigDeleteSomeResponse {
	LicenseAssignmentKey key = 1;
	string error = 2;
};

message LicenseAssignmentConfigDeleteAllRequest {
	// PartialEqFilter provides a way to server-side filter a DeleteAll.
	// This requires all provided fields to be equal to the response.
	// A filtered DeleteAll will use GetAll with filter to find things to delete.
	repeated LicenseAssignmentConfig partial_eq_filter = 1;
};

message LicenseAssignmentConfigDeleteAllResponse {
	// This describes the class of delete error.
	// A DeleteAllResponse is only sent when there is an error.
	fmp.DeleteError type = 1;
	// This indicates the error message from the delete failure.
	google.protobuf.StringValue error = 2;
	// This is the key of the LicenseAssignmentConfig instance that failed to be deleted.
	LicenseAssignmentKey key = 3;
	// Time indicates the (UTC) timestamp when the key was being deleted.
	google.protobuf.Timestamp time = 4;
};

service LicenseAssignmentConfigService {
	rpc GetOne (LicenseAssignmentConfigRequest) returns (LicenseAssignmentConfigResponse);
	rpc GetSome (LicenseAssignmentConfigSomeRequest) returns (stream LicenseAssignmentConfigSomeResponse);
	rpc GetAll (LicenseAssignmentConfigStreamRequest) returns (stream LicenseAssignmentConfigStreamResponse);
	rpc Subscribe (LicenseAssignmentConfigStreamRequest) returns (stream LicenseAssignmentConfigStreamResponse);
	rpc GetMeta (LicenseAssignmentConfigStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (LicenseAssignmentConfigStreamRequest) returns (stream MetaResponse);
	rpc Set (LicenseAssignmentConfigSetRequest) returns (LicenseAssignmentConfigSetResponse);
	rpc SetSome (LicenseAssignmentConfigSetSomeRequest) returns (stream LicenseAssignmentConfigSetSomeResponse);
	rpc Delete (LicenseAssignmentConfigDeleteRequest) returns (LicenseAssignmentConfigDeleteResponse);
	rpc DeleteSome (LicenseAssignmentConfigDeleteSomeRequest) returns (stream LicenseAssignmentConfigDeleteSomeResponse);
	rpc DeleteAll (LicenseAssignmentConfigDeleteAllRequest) returns (stream LicenseAssignmentConfigDeleteAllResponse);
	rpc GetAllBatched (LicenseAssignmentConfigBatchedStreamRequest) returns (stream LicenseAssignmentConfigBatchedStreamResponse);
	rpc SubscribeBatched (LicenseAssignmentConfigBatchedStreamRequest) returns (stream LicenseAssignmentConfigBatchedStreamResponse);
}

message LicenseFileRequest {
	// Key uniquely identifies a LicenseFile instance to retrieve.
	// This value must be populated.
	LicenseFileKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message LicenseFileResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	LicenseFile value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// LicenseFile instance in this response.
	google.protobuf.Timestamp time = 2;
};

message LicenseFileSomeRequest {
	repeated LicenseFileKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message LicenseFileSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	LicenseFile value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	// Time carries the (UTC) timestamp of the last-modification of the
	// LicenseFile instance in this response.
	google.protobuf.Timestamp time = 3;
};

message LicenseFileStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated LicenseFile partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each LicenseFile at end.
	//     * Each LicenseFile response is fully-specified (all fields set).
	//   * start: Returns the state of each LicenseFile at start, followed by updates until now.
	//     * Each LicenseFile response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each LicenseFile at start, followed by updates
	//     until end.
	//     * Each LicenseFile response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message LicenseFileStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	LicenseFile value = 1;

	// Time holds the timestamp of this LicenseFile's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the LicenseFile value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message LicenseFileBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated LicenseFile partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each LicenseFile at end.
	//     * Each LicenseFile response is fully-specified (all fields set).
	//   * start: Returns the state of each LicenseFile at start, followed by updates until now.
	//     * Each LicenseFile response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each LicenseFile at start, followed by updates
	//     until end.
	//     * Each LicenseFile response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message LicenseFileBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated LicenseFileStreamResponse responses = 1;
};

service LicenseFileService {
	rpc GetOne (LicenseFileRequest) returns (LicenseFileResponse);
	rpc GetSome (LicenseFileSomeRequest) returns (stream LicenseFileSomeResponse);
	rpc GetAll (LicenseFileStreamRequest) returns (stream LicenseFileStreamResponse);
	rpc Subscribe (LicenseFileStreamRequest) returns (stream LicenseFileStreamResponse);
	rpc GetMeta (LicenseFileStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (LicenseFileStreamRequest) returns (stream MetaResponse);
	rpc GetAllBatched (LicenseFileBatchedStreamRequest) returns (stream LicenseFileBatchedStreamResponse);
	rpc SubscribeBatched (LicenseFileBatchedStreamRequest) returns (stream LicenseFileBatchedStreamResponse);
}

message LicenseFileConfigRequest {
	// Key uniquely identifies a LicenseFileConfig instance to retrieve.
	// This value must be populated.
	LicenseFileKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message LicenseFileConfigResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	LicenseFileConfig value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// LicenseFileConfig instance in this response.
	google.protobuf.Timestamp time = 2;
};

message LicenseFileConfigSomeRequest {
	repeated LicenseFileKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message LicenseFileConfigSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	LicenseFileConfig value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	// Time carries the (UTC) timestamp of the last-modification of the
	// LicenseFileConfig instance in this response.
	google.protobuf.Timestamp time = 3;
};

message LicenseFileConfigStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated LicenseFileConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each LicenseFileConfig at end.
	//     * Each LicenseFileConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each LicenseFileConfig at start, followed by updates until now.
	//     * Each LicenseFileConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each LicenseFileConfig at start, followed by updates
	//     until end.
	//     * Each LicenseFileConfig response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message LicenseFileConfigStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	LicenseFileConfig value = 1;

	// Time holds the timestamp of this LicenseFileConfig's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the LicenseFileConfig value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message LicenseFileConfigBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated LicenseFileConfig partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each LicenseFileConfig at end.
	//     * Each LicenseFileConfig response is fully-specified (all fields set).
	//   * start: Returns the state of each LicenseFileConfig at start, followed by updates until now.
	//     * Each LicenseFileConfig response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each LicenseFileConfig at start, followed by updates
	//     until end.
	//     * Each LicenseFileConfig response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message LicenseFileConfigBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated LicenseFileConfigStreamResponse responses = 1;
};

message LicenseFileConfigSetRequest {
	// LicenseFileConfig carries the value to set into the datastore.
	// See the documentation on the LicenseFileConfig struct for which fields are required.
	LicenseFileConfig value = 1;
};

message LicenseFileConfigSetResponse {
	// Value carries all the values given in the LicenseFileConfigSetRequest as well
	// as any server-generated values.
	LicenseFileConfig value = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// creation. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==CreatedAt will include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message LicenseFileConfigSetSomeRequest {
	// value contains a list of LicenseFileConfig values to write.
	// It is possible to provide more values than can fit within either:
	//     - the maxiumum send size of the client
	//     - the maximum receive size of the server
	// If this error occurs you must reduce the number of values sent.
	// See gRPC "maximum message size" documentation for more information.
	repeated LicenseFileConfig values = 1;
};

message LicenseFileConfigSetSomeResponse {
	LicenseFileKey key = 1;
	string error = 2;
};

message LicenseFileConfigDeleteRequest {
	// Key indicates which LicenseFileConfig instance to remove.
	// This field must always be set.
	LicenseFileKey key = 1;
};

message LicenseFileConfigDeleteResponse {
	// Key echoes back the key of the deleted LicenseFileConfig instance.
	LicenseFileKey key = 1;

	// Time indicates the (UTC) timestamp at which the system recognizes the
	// deletion. The only guarantees made about this timestamp are:
	//
	//    - it is after the time the request was received
	//    - a time-ranged query with StartTime==DeletedAt will not include this instance.
	//
	google.protobuf.Timestamp time = 2;
};

message LicenseFileConfigDeleteSomeRequest {
	// key contains a list of LicenseFileConfig keys to delete
	repeated LicenseFileKey keys = 1;
};

// LicenseFileConfigDeleteSomeResponse is only sent when there is an error.
message LicenseFileConfigDeleteSomeResponse {
	LicenseFileKey key = 1;
	string error = 2;
};

message LicenseFileConfigDeleteAllRequest {
	// PartialEqFilter provides a way to server-side filter a DeleteAll.
	// This requires all provided fields to be equal to the response.
	// A filtered DeleteAll will use GetAll with filter to find things to delete.
	repeated LicenseFileConfig partial_eq_filter = 1;
};

message LicenseFileConfigDeleteAllResponse {
	// This describes the class of delete error.
	// A DeleteAllResponse is only sent when there is an error.
	fmp.DeleteError type = 1;
	// This indicates the error message from the delete failure.
	google.protobuf.StringValue error = 2;
	// This is the key of the LicenseFileConfig instance that failed to be deleted.
	LicenseFileKey key = 3;
	// Time indicates the (UTC) timestamp when the key was being deleted.
	google.protobuf.Timestamp time = 4;
};

service LicenseFileConfigService {
	rpc GetOne (LicenseFileConfigRequest) returns (LicenseFileConfigResponse);
	rpc GetSome (LicenseFileConfigSomeRequest) returns (stream LicenseFileConfigSomeResponse);
	rpc GetAll (LicenseFileConfigStreamRequest) returns (stream LicenseFileConfigStreamResponse);
	rpc Subscribe (LicenseFileConfigStreamRequest) returns (stream LicenseFileConfigStreamResponse);
	rpc GetMeta (LicenseFileConfigStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (LicenseFileConfigStreamRequest) returns (stream MetaResponse);
	rpc Set (LicenseFileConfigSetRequest) returns (LicenseFileConfigSetResponse);
	rpc SetSome (LicenseFileConfigSetSomeRequest) returns (stream LicenseFileConfigSetSomeResponse);
	rpc Delete (LicenseFileConfigDeleteRequest) returns (LicenseFileConfigDeleteResponse);
	rpc DeleteSome (LicenseFileConfigDeleteSomeRequest) returns (stream LicenseFileConfigDeleteSomeResponse);
	rpc DeleteAll (LicenseFileConfigDeleteAllRequest) returns (stream LicenseFileConfigDeleteAllResponse);
	rpc GetAllBatched (LicenseFileConfigBatchedStreamRequest) returns (stream LicenseFileConfigBatchedStreamResponse);
	rpc SubscribeBatched (LicenseFileConfigBatchedStreamRequest) returns (stream LicenseFileConfigBatchedStreamResponse);
}

message PurchasedLicenseRequest {
	// Key uniquely identifies a PurchasedLicense instance to retrieve.
	// This value must be populated.
	PurchasedLicenseKey key = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message PurchasedLicenseResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	PurchasedLicense value = 1;

	// Time carries the (UTC) timestamp of the last-modification of the
	// PurchasedLicense instance in this response.
	google.protobuf.Timestamp time = 2;
};

message PurchasedLicenseSomeRequest {
	repeated PurchasedLicenseKey keys = 1;

	// Time indicates the time for which you are interested in the data.
	// If no time is given, the server will use the time at which it makes the request.
	google.protobuf.Timestamp time = 2;
};

message PurchasedLicenseSomeResponse {
	// Value is the value requested.
	// This structure will be fully-populated as it exists in the datastore. If
	// optional fields were not given at creation, these fields will be empty or
	// set to default values.
	PurchasedLicense value = 1;

	// Error is an optional field.
	// It should be filled when there is an error in the GetSome process.
	google.protobuf.StringValue error = 2;
	// Time carries the (UTC) timestamp of the last-modification of the
	// PurchasedLicense instance in this response.
	google.protobuf.Timestamp time = 3;
};

message PurchasedLicenseStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated PurchasedLicense partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each PurchasedLicense at end.
	//     * Each PurchasedLicense response is fully-specified (all fields set).
	//   * start: Returns the state of each PurchasedLicense at start, followed by updates until now.
	//     * Each PurchasedLicense response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each PurchasedLicense at start, followed by updates
	//     until end.
	//     * Each PurchasedLicense response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;
};

message PurchasedLicenseStreamResponse {
	// Value is a value deemed relevant to the initiating request.
	// This structure will always have its key-field populated. Which other fields are
	// populated, and why, depends on the value of Operation and what triggered this notification.
	PurchasedLicense value = 1;

	// Time holds the timestamp of this PurchasedLicense's last modification.
	google.protobuf.Timestamp time = 2;

	// Operation indicates how the PurchasedLicense value in this response should be considered.
	// Under non-subscribe requests, this value should always be INITIAL. In a subscription,
	// once all initial data is streamed and the client begins to receive modification updates,
	// you should not see INITIAL again.
	arista.subscriptions.Operation type = 3;
};

message PurchasedLicenseBatchedStreamRequest {
	// PartialEqFilter provides a way to server-side filter a GetAll/Subscribe.
	// This requires all provided fields to be equal to the response.
	//
	// While transparent to users, this field also allows services to optimize internal
	// subscriptions if filter(s) are sufficiently specific.
	repeated PurchasedLicense partial_eq_filter = 1;

	// TimeRange allows limiting response data to within a specified time window.
	// If this field is populated, at least one of the two time fields are required.
	//
	// For GetAll, the fields start and end can be used as follows:
	//
	//   * end: Returns the state of each PurchasedLicense at end.
	//     * Each PurchasedLicense response is fully-specified (all fields set).
	//   * start: Returns the state of each PurchasedLicense at start, followed by updates until now.
	//     * Each PurchasedLicense response at start is fully-specified, but updates may be partial.
	//   * start and end: Returns the state of each PurchasedLicense at start, followed by updates
	//     until end.
	//     * Each PurchasedLicense response at start is fully-specified, but updates until end may
	//       be partial.
	arista.time.TimeBounds time = 3;

	// MaxMessages limits the maximum number of messages that can be contained in one batch.
	// MaxMessages is required to be at least 1.
	// The maximum number of messages in a batch is min(max_messages, INTERNAL_BATCH_LIMIT)
	// INTERNAL_BATCH_LIMIT is set based on the maximum message size.
	google.protobuf.UInt32Value max_messages = 4;
};

message PurchasedLicenseBatchedStreamResponse {
	// Values are the values deemed relevant to the initiating request.
	// The length of this structure is guaranteed to be between (inclusive) 1 and
	// min(req.max_messages, INTERNAL_BATCH_LIMIT).
	repeated PurchasedLicenseStreamResponse responses = 1;
};

service PurchasedLicenseService {
	rpc GetOne (PurchasedLicenseRequest) returns (PurchasedLicenseResponse);
	rpc GetSome (PurchasedLicenseSomeRequest) returns (stream PurchasedLicenseSomeResponse);
	rpc GetAll (PurchasedLicenseStreamRequest) returns (stream PurchasedLicenseStreamResponse);
	rpc Subscribe (PurchasedLicenseStreamRequest) returns (stream PurchasedLicenseStreamResponse);
	rpc GetMeta (PurchasedLicenseStreamRequest) returns (MetaResponse);
	rpc SubscribeMeta (PurchasedLicenseStreamRequest) returns (stream MetaResponse);
	rpc GetAllBatched (PurchasedLicenseBatchedStreamRequest) returns (stream PurchasedLicenseBatchedStreamResponse);
	rpc SubscribeBatched (PurchasedLicenseBatchedStreamRequest) returns (stream PurchasedLicenseBatchedStreamResponse);
}
